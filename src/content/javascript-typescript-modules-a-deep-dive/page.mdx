export const metadata = {
  title: '5 lines of simple but complex JavaScript & TypeScript code',
  description:
    'Understanding module systems like CommonJS and ES modules, goes beyond understanding the syntax. Knowing the history and workings of these systems will help you understand why we are where we are today. It will help you resolve the complex module errors that even AI cannot fix 😉',
  readMoreText: "5 lines can't be that hard, let me see",
  date: new Date('2025-06-18'),
  tags: ['javascript', 'typescript', 'modules', 'history'],
}

# {metadata.title}

In JavaScript and TypeScript, it is possible to write just a couple of lines of "simple" code which is so complex under the hood, that I decided to write this entire blog post about it.

Let's start with the TypeScript code itself, containing a total of 5 lines, spread over 3 files:

<CodeBlock example="javascript-typescript-modules-a-deep-dive/examples/1-combining-cjs-and-esm" />

Can you spot the complexity? ☝️

In the `index.ts` file, we are importing the `add` function from the `add.ts` file.
But to access the `A` and `B` constants, a `require` statement is used.
This means we are trying to combine two different module systems: CommonJS and ES modules.

_**Let's figure out in this blog if we can get this code to run.**_

<Info>
  Since node 24, it is possible to run TypeScript code directly without compiling it first. It does
  so by stripping the type annotations from the code.
</Info>

<TerminalCommandRunner
  command="node index.ts"
  example="1-combining-cjs-and-esm"
  mockedOutputExample="javascript-typescript-modules-a-deep-dive/examples/2-node-output"
/>

Apparently it is not able to find a file called `add`. That's correct: the file is called `add.ts`, not `add`.
Importing with or without extension is a whole other topic on its own, but let's not get distracted ☁︎ Let's fix the issue instead!

<CodeBlock example="javascript-typescript-modules-a-deep-dive/examples/3-correct-imports" />

<TerminalCommandRunner
  command="node index.ts"
  example="3-correct-imports"
  mockedOutputExample="javascript-typescript-modules-a-deep-dive/examples/4-node-output"
/>

Looks like we have our answer: It does _not_ seem possible to combine `require` and `import` statements in the same TypeScript file.

...or is it? Let's continue our search.

<ColoredH2>Module Type Detection</ColoredH2>

The error we got in our last run was quite clear:

<Error>ReferenceError: require is not defined in ES module scope, you can use import instead</Error>

So it should be quite straightforward to resolve it.

<CodeBlock example="javascript-typescript-modules-a-deep-dive/examples/5-all-import" />

<TerminalCommandRunner
  command="node index.ts"
  example="5-all-import"
  mockedOutputExample="javascript-typescript-modules-a-deep-dive/examples/6-node-output"
/>

That works! 🎉

It makes sense to use `import` statements here, because the other files were ES Modules as well, using `export` statements.
But let's challenge this error message anyways. It said we were "in ES module scope". What does that mean?
How does it know that? Is that the default scope for TypeScript?
We can figure this out by changing all `import` statements to `require` statements:

<CodeBlock example="javascript-typescript-modules-a-deep-dive/examples/7-all-require" />

<TerminalCommandRunner
  command="node index.ts"
  example="7-all-require"
  mockedOutputExample="javascript-typescript-modules-a-deep-dive/examples/6-node-output"
/>

Guess what, that works too! 🤯 Notice that we are not just requiring another CommonJS module here. We are actually requiring an _ES module_!
Under the hood, Node.js is behaving very similar to using a dynamic `import()` statement here, but instead it loads synchronously.

<Note>
  `require()`'ing ES modules is only supported since Node.js 22.0 using the <br />
  `--no-experimental-require-module` flag, or since Node.js 22.12 without the flag.
</Note>

Somehow, Node.js decided to treat the `index.ts` file as CommonJS module, and the `require`d modules as ES modules. This is the result of Node.js' _module type detection algorithm_.
It is actually quite simple. It checks the following conditions in order:

{/* TODO: Visualize this */}

1. Does the file have a `.mjs` (or `.mts`) extension? -> **ES module**
2. Does the file have a `.cjs` (or `.cts`) extension? -> **CommonJS module**
3. Does the closest `package.json` file have a `"type": "module"` field? -> **ES module**
4. Does the closest `package.json` not contain `"type": "commonjs"` and does the module contain ES Module syntax (import/export)? -> **ES module**
5. Otherwise -> **CommonJS module**

{/* TODO: Create quiz or code editor to test knowledge */}

There is one caveat though: since the `require()` statement is synchronous, it is not possible to load ES Modules that use top-level `await` statements.
This will throw a clear error:

{/* TODO: Keep order of files but select 2nd file by default */}

<CodeBlock example="javascript-typescript-modules-a-deep-dive/examples/9-top-level-await" />

<TerminalCommandRunner
  command="node index.ts"
  example="9-top-level-await"
  mockedOutputExample="javascript-typescript-modules-a-deep-dive/examples/10-node-output"
/>

As the error suggests, using `import()` instead of `require()` will fix the issue.

<CodeBlock example="javascript-typescript-modules-a-deep-dive/examples/11-import-fix" />

<TerminalCommandRunner
  command="node index.ts"
  example="11-import-fix"
  mockedOutputExample="javascript-typescript-modules-a-deep-dive/examples/6-node-output"
/>

Remember that we were initially trying to combine two different module systems in a single file?
The above code example showcases another capability in Node.js related to that question!
In this file, we are combining a `require` statement with an `import` statement.
Not a regular `import` statement, but a _dynamic_ one.
The reason why this works is because `import()` is just another function call. It is valid JavaScript, no matter the module type.
The difference is that `import()` is an _asynchronous_ function call that runs during _runtime_,
while a regular `import` statement does not "run" at all.

<Note>
  The `import()` function is not available in the browser. It is only available in Node.js.
</Note>

It feels like we are getting closer to the answer, but unfortunately, this is all what's possible using plain Node.js.
However! There is a tool that you get access to when you install TypeScript, that can help us with our problem.
Let's see how that works.

<ColoredH2>TypeScript Compiler</ColoredH2>

<TerminalCommandRunner command="tsc index.ts constants.ts add.ts" example="12-tsc" />

To be continued...

{/* https://nodejs.org/api/modules.html#loading-ecmascript-modules-using-require */}
